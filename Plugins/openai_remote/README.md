# OpenAI Remote Plugin

This plugin adds API routes that call OpenAI's API and ground responses with chunks from the local vector index.

Endpoints:
- `GET /plugins/openai-remote/health`
- `POST /plugins/openai-remote/query`

Settings are managed in the GMTools Plugin Settings page.

