# Fantasy Setting Config

Shared fantasy configuration lives here and can be reused by multiple fantasy worlds.

Current contents:
- `11_professions.yaml` (setting-level professions)

World-specific changes should go under:
- `config/worlds/<world_id>/`
