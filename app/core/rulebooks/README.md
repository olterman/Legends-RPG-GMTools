# Rulebooks

Generic rulebook and reference-document helpers live here.

The core owns:
- loading addon-owned markdown rulebooks
- extracting heading anchors
- generating table-of-contents entries
- producing a readable document model for the site

The core does not own:
- sourcebook-specific parsing rules
- ruleset semantics
- addon import logic
