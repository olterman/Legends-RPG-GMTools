# CSRD Source

This folder contains source material owned by the `csrd` addon.

## Rule
If a raw document or source asset belongs to a specific addon, it should live in that addon's `source/` folder.

## Current Source Material
- `Cypher-System-Reference-Document-2025-08-22.docx`

Imported records should still flow through the normal `GMForge` content pipeline rather than being stored directly here.
