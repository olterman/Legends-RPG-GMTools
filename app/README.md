# GMForge App

This folder contains the rebuilt application code for `GMForge`.

## Layout
- `core/` - system-agnostic platform code
- `systems/` - rules-system packages such as `cypher`
- `plugins/` - optional integrations and tooling

The legacy app remains outside this tree during the rebuild.
